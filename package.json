{
  "name": "drop-calculator",
  "version": "1.0.0",
  "private": true,
  "description": "Offline rifle & pistol ballistics drop calculator",
  "scripts": {
    "copy:www": "rm -rf www && mkdir -p www && cp index.html manifest.json icon-192.png icon-512.png apple-touch-icon.png favicon-32.png www/",
    "sync": "npm run copy:www && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  }
}
